{% block checkbox_row %}
    <div class="ch-cookie-consent__category">
        <div class="ch-cookie-consent__category-toggle form-switch fs-4">
            {{ form_widget(form) }}
            {# {{ form_label(form, null, {translation_domain: 'CHCookieConsentBundle') }} #}
        </div>
        <div class="ch-cookie-consent__category-information">
            <h5 class="ch-cookie-consent__category-title">{{ ('ch_cookie_consent.'~name~'.title')|trans({}, 'CHCookieConsentBundle') }}</h5>
            <p class="ch-cookie-consent__category-description">{{ ('ch_cookie_consent.'~name~'.description')|trans({}, 'CHCookieConsentBundle') }}</p>
        </div>
    </div>
{% endblock %}

{% block submit_row %}
    <div class="ch-cookie-consent__btn-wrapper">
        {{ block('button_widget') }}
    </div>
{% endblock %}
